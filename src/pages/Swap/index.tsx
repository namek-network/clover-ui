import React, { Component } from 'react';
import styled from 'styled-components';

const TempDiv = styled.div`
  // min-height: 30px;
  // min-width: 300px;
`;

export default class Swap extends Component {
  render() {
    return (
      <TempDiv>Swap page placeholder</TempDiv>
    );
  }
}
