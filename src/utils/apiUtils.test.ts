import { api, initApi } from './apiUtils';

describe('apiUtils test suite', () => {

  test('toAddLiquidity', async () => {
    // will time out...
    // await initApi(() => {})
    // const data = await api.toAddLiquidity("BXB", "BUSD", '1000000000000', '20000000000000')
    // expect(data).toBe('peanut butter');
  });

});
